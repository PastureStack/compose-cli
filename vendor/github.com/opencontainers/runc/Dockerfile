FROM ubuntu:26.04

RUN set -eux; \
    echo "The vendored runc test image build path is intentionally disabled."; \
    echo "It is not part of the Rancher compose-executor maintenance build."; \
    exit 1
